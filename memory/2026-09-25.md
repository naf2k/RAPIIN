# 2026-09-25

## Audit + perbaikan RAPIIN (`/Users/haimac/orca/projects/rapiin`, remote `github.com/naf2k/RAPIIN`)

Diminta owner: audit lalu "selesaikan semua". Audit read-only dulu, lalu perbaikan dieksekusi dengan persetujuan eksplisit.

### Bukti terverifikasi (jalankan ulang kalau ragu) <!-- project: github.com/naf2k/RAPIIN -->
- `cd server && uv run pytest -q` → **159 passed**. `cd agent && uv run pytest -q` → **38 passed**. `cd frontend && npm run build` → sukses (2296 modul, `index` 493 kB / 151 kB gzip).
- Service live: API `server/run.py` (port 8000), `ops/run_queue_worker.py`, agent CLI, 9router di 20128. Postgres (`beresin_local`) + Redis OK.
- `server/.env` mode 0600 + gitignored; tidak ada secret ter-commit.

### P0 — CI (SUDAH DIPERBAIKI) <!-- project: github.com/naf2k/RAPIIN -->
Root cause: `notify-operations.needs` menyebut job `frontend` yang sudah dihapus → GitHub **menolak parse** workflow. Bukti: run bernama `.github/workflows/ci.yml` dengan **0 job**. Bukan "sebagian job gagal".
Fix: tambah job `frontend` (`npm ci` + `npm run build` + upload dist) sehingga `needs: [server, agent, frontend, docker]` valid. Diverifikasi: 5 job terdefinisi, semua `needs` resolve, YAML valid.
Catatan: run CI **sebelum** fix (sha cb7ab73) sudah `failure`; fix dikirim setelahnya.

### P1 — runtime (SUDAH DIPERBAIKI sebagian) <!-- project: github.com/naf2k/RAPIIN -->
- `seed_ops()` idempotent: fast-path key-based (`set(ROLE_POLICIES) ⊆ roles AND set(POLICY_KEYS) ⊆ keys` → return). Menghapus upsert berulang tiap dispatch/tick yang menyebabkan `psycopg.errors.LockNotAvailable`. `POLICY_KEYS` dijaga sinkron dengan `defaults` via assert.
- Log: `ops/rotate_logs.sh` + LaunchAgent `com.rapiin.logrotate` (harian 04:15). **Truncate in-place, bukan rename** — launchd `StandardOutPath` memegang fd, jadi newsyslog/rename akan membuat log tidak pernah kembali. Sudah diuji jalan.
- Disk: masih **~5.4%** (6.1Gi). Truncate 30MB `server.log` hampir tidak menggerakkan angka → **log BUKAN penyebab** insiden #84. Konsumen sebenarnya: `~/Library` 11G (`Application Support` 4.7G, `pnpm` 2.3G, `Caches` 2.2G) dan `~/orca` 7.4G. Insiden #84 masih OPEN; `sudo` butuh password → config sistem tidak tersentuh.

### P2 — jalur produksi UI (SUDAH DIBUAT) <!-- project: github.com/naf2k/RAPIIN -->
Keputusan arsitektur mengikuti tes yang ada: `tests/test_production_controls.py::test_api_only_server_does_not_serve_frontend_assets` menyandikan **"server harus API-only"**. Jadi frontend TIDAK ditempel ke FastAPI. Yang dibuat: `Dockerfile.frontend` (node build → nginx), `deploy/nginx.conf` (serve SPA + proxy `/api`, `proxy_buffering off` untuk SSE, SPA fallback), service `frontend` di `docker-compose.production.yml`, Caddy dialihkan ke `frontend:8080`.
**Belum terverifikasi**: `docker` tidak terpasang di mesin ini → image belum pernah dibangun/diuji.

### P3 — kebersihan (SUDAH) <!-- project: github.com/naf2k/RAPIIN -->
- `.env`: hapus 2 baris duplikat — `RAPIIN_INIT_SUPERVISOR_EMAIL` (efektif `admin@beresin.example.com`) dan `OPS_AGENT_MONTHLY_COST_LIMIT_USD`. Backup: `server/.env.bak-20260925`. Nilai efektif tidak berubah.
- Docs: `install_agent_release.sh` → `install_rapiin_release.sh` di README + `docs/SECOND_DEVICE_RUNBOOK.md`.
- `.VIEW/` ditambahkan ke `.gitignore`. Artefak basi (`server/dist`, `agent/dist`, 4 `*.egg-info`, `beresin-agent-local.spec`) dibuang ke Trash.
- `bin/moon.png` di-vendor (5.3MB → 2.4MB, 2560×1440) menggantikan CDN `cdn.21st.dev` di `ruixen-moon-chat.tsx` + `LoginPage.tsx`. **CSP `img-src 'self' data:` berarti gambar CDN itu tidak akan pernah render di produksi** — ini bug nyata yang ikut diperbaiki.
- Lint: `.editorconfig` + `[tool.ruff]` line-length saja (tanpa `lint.select`) — pass pertama menemukan **492** temuan di server dan **52** di agent; gating CI akan langsung mematikan pipeline. Frontend masih tanpa tes (belum ada `test` script).

### Koreksi catatan audit awal (PENTING) <!-- project: github.com/naf2k/RAPIIN -->
- ~~`permissions.py:sandbox_root()` dead code~~ → **SALAH**. Dipakai di `tools/analysis.py`, `registry.py`, `filesystem.py`, `duplicate.py`, `organizer.py`, `agent/core.py` + 4 file tes. Jangan dihapus. Fallback `Path.home()` memang ada di kandidat root (`_DEFAULT_ROOT_CANDIDATES`) tapi tidak otomatis terpakai bila `RAPIIN_SANDBOX_ROOT` diisi.
- ~~`PRD-RAPIIN.md` ≈ `frontend/PRD.md` identik~~ → **SALAH**. Ukuran beda (39990 vs 39999 byte), mtime beda. Bukan duplikat identik.
- "frontend tanpa tes" masih benar.

### Belum selesai / risiko terbuka
- **CI: job tidak pernah dapat runner.** Fix job graph sudah benar — run terbaru (81c2a34) sekarang bernama `CI` (bukan path file), artinya workflow diterima dan dijalankan. Tapi **semua job gagal dalam 3 detik dengan `runner_name=""` dan 0 langkah**. Bandingkan run sukses 10 Sep: `runner_name='GitHub Actions 1000000856'`, 9–14 langkah. Batasnya jelas — run 10 Sep 14:53 dapat runner & sukses; run 13 Sep 07:12 dan sesudahnya `runner='' steps=0`. Semua job (server, agent, frontend, docker, notify) dan workflow Security kena pola sama.
  **Ini masalah infrastruktur akun GitHub, bukan kode.** Dugaan: GitHub Actions dinonaktifkan karena masalah billing/pembayaran, atau habisnya kuota menit private repo (20 Des 2025 lalu repo ini commit-nya jadi PAID). Hanya bisa dipastikan di github.com → Settings → Billing → Actions. Token CLI tidak punya izin `checks:read` maupun billing, dan log job tidak tersedia (`BlobNotFound`).
  Sampai ini dibereskan, tes CI tidak akan pernah jalan walau kode sudah bersih.
- Insiden #84 (disk) masih OPEN; butuh keputusan owner (bersihkan `~/Library` mana).
- `ops/run_queue_worker.py`, incident #45 (desktop agent offline 8536×), #51 (backup verification) belum ditangani. Backup terakhir 2026-09-24.
- Image frontend belum dibangun (tidak ada docker di mesin ini).
